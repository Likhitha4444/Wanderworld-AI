# Project Workflow

## Development
- Use `venv` for Python dependencies (`backend/`).
- Use `npm` or `yarn` for React dependencies (`frontend/`).

## Git
- Do not commit secrets, `.env` files, or build artifacts.
- Branching strategy: `main` (production), `develop` (integration), `feature/xxx`.

## Commits
- Use conventional commits.
